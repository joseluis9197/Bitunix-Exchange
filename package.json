{
  "name": "bitunix-exchange",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hostable multi-user grid trading bot for Bitunix Futures, with real-time dashboard and Telegram alerts.",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://bots.prosperollc.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/joseluis9197/Bitunix-Exchange.git"
  },
  "bugs": {
    "url": "https://github.com/joseluis9197/Bitunix-Exchange/issues"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:bot": "npm run dev --workspace=@grvt-grid/bot",
    "dev:dashboard": "npm run dev --workspace=@grvt-grid/dashboard",
    "dev:notifier": "npm run dev --workspace=@grvt-grid/notifier",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
